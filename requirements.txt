# AI Vocals Studio - Requirements
# ===============================

# Core dependencies
# tkinter - built into Python, not installable via pip
gTTS>=2.3.0
pydub>=0.25.1
numpy>=1.21.0
soundfile>=0.12.0
librosa>=0.10.0
Pillow>=9.0.0
streamlit>=1.48.0
pyworld>=0.3.5
demucs>=4.0.0

# For cloud training (optional but recommended)
kaggle>=1.5.0
requests>=2.28.0

# Qwen3-TTS rapid reference voice cloning (web app advanced engine)
qwen-tts>=0.1.1

# ElevenLabs — best-in-class voice cloning (primary engine, requires API key)
# Get key at elevenlabs.io → Profile → API Keys ($5/mo Starter plan minimum)
elevenlabs>=1.0.0

# XTTS v2 and RVC are installed through install_voice_engines.sh.
# They currently require a Python 3.10/3.11 sidecar; this main app venv is
# Python 3.12-compatible and will use Qwen3-TTS, ElevenLabs, WORLD/DSP, and
# Demucs directly.

# For voice conversion (optional - for local preprocessing)
# so-vits-svc-fork==4.2.30
# torchcodec

# Development dependencies (optional)
# pytest>=7.0.0
# black>=22.0.0
