# AI Vocals Studio - Requirements
# ===============================

# Core dependencies
tkinter
gTTS>=2.3.0
pydub>=0.25.1
numpy>=1.21.0
soundfile>=0.12.0
librosa>=0.10.0
Pillow>=9.0.0

# For cloud training (optional but recommended)
kaggle>=1.5.0
requests>=2.28.0

# ElevenLabs — best-in-class voice cloning (primary engine, requires API key)
# Get key at elevenlabs.io → Profile → API Keys ($5/mo Starter plan minimum)
elevenlabs>=1.0.0

# XTTS v2 zero-shot voice cloning (free fallback — runs locally, no API key)
# Downloads ~1.8 GB XTTS v2 model on first use
TTS>=0.22.0

# RVC v2 — best audio-to-audio voice conversion (for rap/singing)
# Install after training a model on Colab
rvc-python>=0.0.17

# For voice conversion (optional - for local preprocessing)
# so-vits-svc-fork==4.2.30
# torchcodec

# Development dependencies (optional)
# pytest>=7.0.0
# black>=22.0.0
